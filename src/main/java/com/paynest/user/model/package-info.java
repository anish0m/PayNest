/**
 * User : firstname, email, password.
 * Plain Java. No Spring, no SQL, no HTTP.
 * Never saving itself to a database, reading a web request.
 * owns identity, not money
 */
package com.paynest.user.model;